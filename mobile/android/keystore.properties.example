# Copy to android/keystore.properties and fill in. This file is git-ignored:
# a release keystore and its passwords must never be committed.
#
# Create the keystore first:
#   bash scripts/make-keystore.sh
#
storeFile=keystore/easy-hotel-booking-release.jks
storePassword=
keyAlias=easyhotelbooking
keyPassword=
