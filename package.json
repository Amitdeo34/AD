{
  "name": "easy-hotel-booking",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Easy Hotel Booking \u2014 hotels, homestays and guest houses across every state, district and village in India",
  "workspaces": [
    "mobile"
  ],
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "node --import ./test/register-alias.js --test test/*.test.js",
    "build:android": "bash scripts/build-android.sh",
    "mobile:sync": "npm run sync --workspace mobile",
    "mobile:icons": "npm run icons --workspace mobile",
    "seed": "node scripts/seed-demo.mjs"
  },
  "dependencies": {
    "next": "^16.3.4",
    "qrcode": "^1.5.4",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}